# daily-ritual

Frontend delivery for the active auth backlog slice of the daily-ritual project.

## Environment

Create a local `.env` file if needed:

```bash
VITE_API_URL=http://localhost:8080
```

## Run

```bash
npm install
npm run dev
```
